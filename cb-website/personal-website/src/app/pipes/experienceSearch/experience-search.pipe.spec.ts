import { ExperienceSearchPipe } from './experience-search.pipe';

describe('ExperienceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
